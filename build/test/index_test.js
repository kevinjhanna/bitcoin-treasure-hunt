"use strict";
const chai = require('chai');
const expect = chai.expect;
describe('describe', () => {
    context('context', () => {
        it('should..', () => {
            expect(1).to.eql(1);
        });
    });
});
//# sourceMappingURL=index_test.js.map